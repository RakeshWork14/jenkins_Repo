// tools
pipeline{
    agent {
        label 'java-slave-label'
    }
    tools {
        maven 'maven-3.9.9'
    }
    stages{
        stage('maven'){
            steps{
                sh "mvn --version"
            }
        }
        stage('java'){
            tools{
                jdk 'JDK'
            }
            steps{
                sh "mvn --version"

            }
        }
    }
}
