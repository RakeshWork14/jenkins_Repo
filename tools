// tools
pipeline{
    agent any
    tools {
        maven 'maven-3.9.9'
    }
    stages{
        stage('maven'){
            steps{
                sh "mvn --version"
            }
        }
        stage('java'){
           agent {
        label 'java-slave-label'
    }
            tools{
                jdk 'JDK'
            }
            steps{
                sh "mvn --version"

            }
        }
    }
}
